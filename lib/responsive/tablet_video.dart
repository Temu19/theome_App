import 'package:flutter/material.dart';

class TabletVideo extends StatefulWidget {
  const TabletVideo({super.key});

  @override
  State<TabletVideo> createState() => _TabletVideoState();
}

class _TabletVideoState extends State<TabletVideo> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.blue,
    );
  }
}